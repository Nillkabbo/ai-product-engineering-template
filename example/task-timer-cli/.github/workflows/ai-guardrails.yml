name: AI Guardrails

on: [pull_request]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Ensure task reference
        run: |
          if ! grep -R "Task:" tasks/; then
            echo "WARNING: No task files found"
            exit 0
          fi
